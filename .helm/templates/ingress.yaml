apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: werf-guide-app
spec:
  rules:
  - host: werf-guide-app.jupiter.icn.su
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: werf-guide-app
            port:
              number: 3000
